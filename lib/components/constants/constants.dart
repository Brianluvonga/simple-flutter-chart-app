import 'package:flutter/material.dart';

// class for some basic flutter colors
class ColorConstants {
  var colorA = Colors.black;
  var colorB = Colors.green;
  var colorC = Colors.grey;
  var colorD = Colors.black;

  ColorConstants();
}

//colors for basic font sizes
class FontSizeConstants {
  TextStyle style = const TextStyle();
  var sizeA = const TextStyle(fontSize: 10);
  var sizeB = const TextStyle(fontSize: 11);
  var sizeC = const TextStyle(fontSize: 12);
  var sizeD = const TextStyle(fontSize: 13);
}
